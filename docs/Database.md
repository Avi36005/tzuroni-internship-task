# Database Schema Documentation

The Weather Prediction AI Trading Agent stores all forecasts, market prices, predictions, trades, and portfolio history in a local SQLite database managed by SQLAlchemy async ORM.

---

## 📊 Entity Relationship Diagram

```mermaid
erDiagram
    CITIES ||--o{ WEATHER_DATA : "has"
    CITIES ||--o{ RESEARCH_RESULTS : "has"
    CITIES ||--o{ MARKETS : "has"
    MARKETS ||--o{ PREDICTIONS : "has"
    MARKETS ||--o{ ORDERS : "has"
    MARKETS ||--o{ POSITIONS : "has"
    PREDICTIONS ||--o{ ORDERS : "generates"
    
    CITIES {
        int id PK
        string name
        string country
        float latitude
        float longitude
        string local_agency
    }
    
    WEATHER_DATA {
        int id PK
        int city_id FK
        string date
        float temperature_max
        float temperature_min
        float precipitation_sum
        float precipitation_probability
        float humidity_mean
        float wind_speed_max
        float pressure_mean
        float uv_index_max
        string extreme_alerts
        datetime fetched_at
    }
    
    RESEARCH_RESULTS {
        int id PK
        int city_id FK
        string date
        float sentiment_score
        string ai_summary
        string sources
        float confidence_score
        datetime fetched_at
    }
    
    MARKETS {
        int id PK
        int city_id FK
        string title
        string slug
        string condition_id
        string clob_token_id_yes
        string clob_token_id_no
        float yes_price
        float no_price
        float implied_probability
        float volume
        float liquidity
        float spread
        string expiration_date
        boolean is_active
        datetime updated_at
    }
    
    PREDICTIONS {
        int id PK
        int market_id FK
        string prediction_date
        float model_probability_yes
        float model_probability_no
        float confidence
        float edge
        float expected_value
        string decision
        string reasoning
        datetime created_at
    }
    
    ORDERS {
        int id PK
        int market_id FK
        int prediction_id FK
        string side
        float price
        float amount
        float cost
        string reason
        string status
        float slippage
        datetime executed_at
    }
    
    POSITIONS {
        int id PK
        int market_id FK
        string side
        float average_price
        float shares
        float current_price
        float pnl
        boolean is_hedged
        datetime updated_at
    }
    
    PORTFOLIO_STATE {
        int id PK
        datetime timestamp
        float cash
        float portfolio_value
        float unrealized_pnl
        float daily_return
        float exposure
        float max_drawdown
    }
```

---

## 🗂️ Core Tables Description

### 1. `cities`
Stores coordinates, names, and regional offices for the 20 supported target weather centers.

### 2. `weather_data`
Logs daily forecast parameters and precipitation probabilities.

### 3. `research_results`
Stores news sentiment, summaries, and source URLs scraped by Agent 4.

### 4. `markets`
Tracks active Polymarket weather contracts, YES/NO prices, book spreads, and volumes.

### 5. `predictions`
Stores calibrated YES/NO probabilities, calculated edge, and EV generated by Agent 5.

### 6. `orders`
Stores transaction details (slippage, cost, size, execution status) placed by Agent 7.

### 7. `positions`
Tracks active holdings (average cost basis, current price, net PnL) in the portfolio.

### 8. `portfolio_state`
A historical time-series of the portfolio value, cash, and drawdown metrics, used to plot the equity curve.
